# Local: point at the Express API
VITE_API_URL=http://localhost:5000/api

# Production (Vercel Services): same-origin API via rewrite — set in Vercel Project → Environment Variables
# VITE_API_URL=/api
